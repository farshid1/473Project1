var geocoderProvider = 'google';
var httpAdapter = 'http';
// optionnal
var extra = {
};

var geocoder = require('./index.js').getGeocoder(geocoderProvider, httpAdapter, extra);

geocoder.reverse(45.767, 4.833).then(function(res) {
    console.log(res);
});